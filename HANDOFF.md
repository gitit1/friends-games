# HANDOFF — assaf-friends-games

הערות חדשות למעלה. קריא בשתי דקות.

---

## 2026-09-07 — רענון ה-README (פנימי + מראה ציבורית)

שני קובצי ה-README היו מיושנים מול הקוד. תוקן בענף `docs/readme-refresh`:

- `README.md` (פנימי): היה **29 משחקים ב-6 קטגוריות** וקישור חי ל-Netlify →
  עכשיו **62 משחקים ב-9 קטגוריות** (מספרים 14 · כיף 11 · אותיות 9 · יצירה 7 ·
  אנגלית 6 · חשיבה 5 · ספורט 4 · גדלים 4 · מוזיקה 2), קישור חי
  https://assaf.gititregev.com, פריסת Docker/nginx דרך Coolify במקום Netlify,
  עקרונות שנוספו מאז (קושי מצטבר + `check:champion`, דוקטרינת הארט האפוי),
  פורט פיתוח 5199, `scripts/sync-public.mjs`, ותיקון הקישור השבור
  ל-`FRIENDS-WORLD.md` (עבר ל-`docs/`) + טבלת תיעוד.
- `README.public.md` (מה שמופיע בריפו הציבורי `gitit1/friends-games`): היה
  **52 משחקים ב-8 קטגוריות** + קישור ל-Netlify → 62/9. **הקישור החי הוסר**
  ולא הוחלף בכתובת החדשה — `assaf.gititregev.com` חושף ברבים את שם הילד ואת
  הדומיין הפרטי, והריפו הופרט ב-15.7 בדיוק מהסיבה הזו. **גיתית הכריעה (7.9):**
  הקישור החי חוזר — לא אכפת לה שהשם מוצג, והכתובת לא נותנת גישה לשרת עצמו.

**מצב פרסום שנבדק ב-7.9:** האתר חי ומגיב (HTTP 200, עברית) ומגיש בדיוק את
הבנייה של 7.8 — אותו קובץ `assets/index-BFiOlgTt.js` שיושב ב-`dist/` המקומי.
אין קוד חדש ב-main מאז (הקומיטים מ-8.8 ואילך הם תיעוד בלבד), ואין ענף עם קוד
שלא מוזג. כלומר: **מה שבאוויר = מה שב-main.**

**בוצע ונסגר באותו יום:** הענף מוזג ל-main, main נדחף ל-origin (מאומת:
אפס קומיטים בפיגור), והמראה הציבורית סונכרנה — 1432 קבצים, ואומת שה-README
בריפו `gitit1/friends-games` מציג 62 משחקים / 9 קטגוריות + הקישור החי.
פריסה מחדש לשרת לא נדרשה: לא השתנה קוד.

## 2026-08-19 — מעבר מ-Netlify לשרת ההאב — **✅ הושלם, האתר חי**

**https://assaf.gititregev.com חי ומאומת** (19.8): HTTP/2 + Let's Encrypt
(תקף עד 17.11.2026), כותרת עברית, JS/CSS עם `immutable`, `index.html`
עם `no-cache`, ארט (`art/bg/autumn.jpg`) וקול (`voice/he/*.mp3`) מוגשים.
עלתה v39 (גיתית דחפה את 40 הקומיטים + ה-Dockerfile).

**ההרכב הסופי:** Coolify app `3vtprtzxwhjhydjp30sedkf4` (פרויקט
`asaf-friends-games`), build_pack=**dockerfile** (`is_static=false` —
חובה! עם true Coolify עוטף ומחפש `/app/dist` ונכשל), deploy key
`asaf-games-deploy-key`. פריסה: push ל-main ואז deploy מה-API (webhook
אוטומטי לא הוגדר בכוונה — הפרסום נשאר ידני כמנהג הריפו).

**✅ החסימה ברשת הביתית נפתרה (19.8):** גיתית הגישה בקשת סיווג-מחדש,
FortiGuard אישרו ("Personal Websites and Blogs"), והתפוצה הגיעה למכשיר
תוך שעות. אומת מהרשת שלה: HTTPS 200, `ssl_verify=0` (אין יירוט), כותרת
עברית, JS/ארט/קול — הכול 200. **הדומיין כולו שוחרר**, כולל
`coolify.gititregev.com` ו-StR.

**נשאר:** (1) מחיקת האתר ב-Netlify (גיתית); (2) דחיפת קומיטי התיעוד.

### ההיסטוריה המלאה של ההקמה (19.8)

גיתית ביקשה לנתק את האתר מ-Netlify ולארח אותו בשרת החדש, בכתובת
`assaf.gititregev.com`.

### מה כבר בוצע (2026-08-19)
1. **DNS מוכן** — רשומת ה-wildcard של הדומיין כבר מפנה את `assaf.gititregev.com`
   ל-204.168.255.68. אין צורך לפתוח סאב-דומיין ידנית.
2. **מפתח פריסה נוצר** בשרת (`/tmp/asaf-key/asaf_deploy`) והציבורי נוסף לריפו
   בגיטהאב כ-deploy key בקריאה-בלבד (`coolify-asaf-deploy`, id 160678571).
3. **פרויקט Coolify נוצר**: `asaf-friends-games`, uuid `svjlzlbhxk3kusziyetzb0mz`.

### מה חסום — צעד אחד לגיתית
מסווג ההרשאות חוסם אותי מכל פקודה שנוגעת בקובץ המפתח הפרטי, ולכן רישום
המפתח ב-Coolify (API `security/keys`) חייב לרוץ ע"י גיתית. הסקריפט מוכן
ב-scratchpad של הסשן (`register-asaf-key.sh`) — scp לשרת + הרצה. אחרי
שהמפתח רשום: ליצור אפליקציה (private-deploy-key, nixpacks static,
`npm run build`, publish `dist`, דומיין `https://assaf.gititregev.com`,
server uuid `ofhn4jlk28uzbvyynhjwbocu`) ולהריץ deploy.

### התקדמות 19.8 בהמשך היום
גיתית הריצה את רישום המפתח (uuid `nd2mqty6btkndkglprbejbuk`). נוצרה אפליקציה
`3vtprtzxwhjhydjp30sedkf4` (nixpacks static, `npm run build`, publish `dist`,
דומיין `https://assaf.gititregev.com`). שתי פריסות נכשלו על גרסת Node:
ברירת המחדל של nixpacks בשרת נותנת Node 22.11.0 בעוד Vite 8 דורש 22.12+
(וגם ה-binding של rolldown לא מותקן כי npm מדלג על אופציונלי שנכשל ב-engines);
`NIXPACKS_NODE_VERSION=23` לא נתמך ונפל ל-18. **הפתרון:** env
`NIXPACKS_NIXPKGS_ARCHIVE=ac62194c3917d5f474c1a844b6fd6da2db95077d`
(nixos-25.05, Node 22.20.0) + `NIXPACKS_NODE_VERSION=22`. **גם זה לא חלחל**
(פריסה שלישית נכשלה, אותו Node 22.11) — nixpacks של השרת מתעלם מהמשתנה.

**הפתרון הסופי: Dockerfile בריפו** (ענף `deploy/hub-server`, מוזג ל-main):
Node 22.20-alpine לבנייה + nginx להגשה (fallback של SPA, קאש immutable
ל-assets, no-cache ל-index). **נבדק בפועל בשרת** — הועלה tar של v39, נבנה
docker image והוגש: כותרת עברית תקינה, assets עם Cache-Control נכון.
האפליקציה ב-Coolify הועברה ל-build_pack=dockerfile. **ממתין לדחיפה של
גיתית** — ואז deploy יעלה את v39 ישירות.

### מוקשים שהתגלו
- **FortiGuard חוסם את הדומיין מהרשת של גיתית!** גלישה ל-`coolify.gititregev.com`
  מהמחשב מוחזרת עם דף חסימה בסיווג שגוי ("Pornography"). עוקף: SSH לשרת
  ו-API דרך localhost:8000. חובה להגיש בקשת סיווג-מחדש — אחרת גם אתר
  המשחקים ייחסם ברשת הביתית.
- origin/main נמצא 40 קומיטים אחרי main המקומי (v38 מול v39) — Coolify בונה
  מגיטהאב, אז עד שגיתית דוחפת, הפריסה תהיה v38.
- ניתוק Netlify בפועל (מחיקת האתר בדשבורד) — רק גיתית, אין לי גישה לחשבון.
  את `netlify.toml` מוחקים מהריפו אחרי שהפריסה החדשה מאומתת.

---

## 2026-08-07 — ביקורת "פונקציונליות שאבדה" — **הסקר הושלם, ממתין להחלטת גיתית**

גיתית זיהתה שהמיון איבד סלים. נסרקו **כל 63 המשחקים** מול היסטוריית הגיט המלאה
(6 סוכני sonnet read-only, כל הממצאים הקריטיים אומתו ידנית על הקוד החי + הקומיטים).

**דוח מלא לגיתית (Artifact):** https://claude.ai/code/artifact/d508e310-8250-471d-b6f3-2e790e298587

### 7 פגיעות שאומתו (אלוף היום < השיא ההיסטורי — הפרת docs/GAME-RULES.md)
| משחק | קובץ | היה → היום | הקומיט הפוגע |
|---|---|---|---|
| מיון | `SortByColor.tsx` | stepper "עוד סל" עד 30 → אלוף 8 | `7afee59` (שיא: `ec1d7a9`) |
| מספר וכמות | `QtyMatch.tsx` | MAXT=30 → QMAX [3,5,8,8] | `432c224` (שיא: `7b5d6be`) |
| זיכרון | `MemoryGame.tsx` | stepper חופשי עד 24 קלפים → [3,6,8,10] זוגות | `b3da6ec` (שיא: `152062e`) |
| חיבור נקודות | `ConnectDots.tsx` | כל הרוסטר (~99 נקודות) → DOT_BANDS מקס ~24 | `7be64d7` |
| פסנתר | `src/music/songs.ts` | 19 שירים → 10 (18 ב-SONG_QUEUE נטושים מיוני) | `c2bb47f` (שיא: `5b5774d`) |
| תבניות | `PatternGame.tsx` | רצף עד 9 → קבוע 6 | `8054eec` |
| פיצוץ חברים | `PopFriends.tsx` | 6×6 → 5×5 (נימוק חושי מתועד) | `f5ac993` |

**56 האחרים נקיים**, חלקם גדלו (BuildNumber עד 100, CalcFriends בלי מגבלת קלט,
CoinSort שמר 1→100). גל הארט עצמו היה ממושמע ("Logic frozen" בקומיטים) — הפוגעים
היו קומיטי דרגות/בנייה־מחדש שמסגרו את הקיצוץ כ"אתגר".

### ✅ ההחלטה התקבלה (גיתית, 2026-08-07) — ביצוע בעבודה
גיתית אישרה את ההמלצה כלשונה. **עדכון חשוב ממנה: אסף משחק היום בעיקר בטאבלט** —
לכן כל החזרת-צפיפות חייבת לנצל מסך רחב (הפריטים גדלים בטאבלט), והאימות בצילום
נעשה גם ב-360px וגם ב-~820px.

### Dispatch ledger — ביצוע ההחזרות · **הכול מוזג ואומת, 2026-08-07**
| WP | מה | סטטוס | קומיט |
|---|---|---|---|
| A1 | מיון: stepper "עוד סל" עד 30, דרגה=נקודת פתיחה | ✅ verified (צולם 360+820) | `1e2b56a` + `959838d` |
| A2 | זיכרון: מצב "חופשי" + stepper עד 24 קלפים | ✅ verified (צולם) | `5fb4197` |
| A3 | חיבור נקודות: דפדוף כל הרוסטר — צולם חבר 100 עם 100 נקודות בטלפון | ✅ verified | `62c9b9b` |
| B1 | מספר וכמות: אלוף 8→30 | ✅ verified (צולם) | `ed05b2a` |
| B2 | תבניות: אלוף רצף משתנה עד 9 | ✅ verified (צולם n=9) | `733519f` |
| C  | פסנתר: 4 שירים קודדו (ode/itsy/happy/bingo) באימות אינטרוולים; wheels+birthday דולגו ביושר (לא נכנסים ב-6 לאנים) | ✅ merged — **ממתין לבדיקת שמיעה של גיתית** | `b2418d9` |
| G  | שומר רגרסיה: `npm run check:champion` — **14/14 OK** | ✅ verified | `e2923c7` + baseline tune ב-`959838d` |

**אימות שעשיתי בעצמי:** tsc+build נקיים; צילומי Chromium ב-360×780 ו-820×1180 לכל
חמשת המשחקים (סקריפטים זמניים, נמחקו; צילומים ב-scratchpad של הסשן). שני באגים
שאני תפסתי ותיקנתי בעצמי (בגבול ה"זעיר"): מגש הכדורים נמעך מאחורי השולחן בטלפון
(ברירת מחדל ובמצב צפוף) — תוקן בכיווץ ה-stepper ל-56px בטלפון, תקרת כדור 56px
במסך נמוך, ובמצב >12 סלים: stage/tray בגובה טבעי + גלילת מסך (`959838d`).

**מוקש שהתגלה: worktree של סוכן מסתעף מ-main (e8750c6), לא מהענף הנוכחי** —
לכן מוזג בקטיף (cherry-pick) קומיט-קומיט, לא במיזוג ענפים. לזכור בגלים הבאים.

### נשאר פתוח אחרי הגל הזה
1. **בדיקת שמיעה של גיתית ל-4 שירי הפסנתר החדשים** (אודה לשמחה, עכביש קטנטן, אם טוב לך, בינגו) — טבלת האינטרוולים בדוח הסוכן; אם שיר צורם — מתקנים/מסירים רק אותו.
2. ליטוש קטן בתבניות-טלפון: ברצף 9 חרוז בודד יורד לשורה שנייה על שפת המדף — קביל, אפשר לרווח בעתיד.
3. ניקוי worktrees: 7 חדשים של הגל הזה (חלקם עם node_modules — **למחוק node_modules לפני** `git worktree remove`, תקרית 2026-07-11) + ~89 ישנים.
4. שרת preview על 6321 אולי עדיין רץ ברקע — לסגור אם מפריע.
5. לא נדחף — הפרסום ידני ע"י גיתית כרגיל.

---

## 2026-08-03 — 13 המשחקים האחרונים שודרגו · **גל האיכות הושלם: 63/63**

### מה נסגר
כל 13 המשחקים שמעולם לא קיבלו ארט אפוי שודרגו ומוזגו ל-`a11y-touch-targets`:

| קטגוריה | משחקים | מה נבנה |
|---|---|---|
| מוזיקה (2) | להקה, מכונת הקצב | במת ערב אפויה עם רצפת פרספקטיבה, צ׳יפי עץ, בלוקים מכופפים + שקעים חרוטים |
| אנגלית (6) | spell, firstletter, rhyme, blend, encount, trace | **קוביות "אותיות חיות" אפויות** עם עיניים — סט ספרייטים אחד משותף שמשפר 4 משחקים בבת אחת + מדף פרספקטיבה |
| גדלים (4) | גמילה, רגשות, סדר יום, עזרה | חדר אמבטיה + אסלה, פינת ויסות רגועה + אביזרי הרגעה, כרטיסי שגרה על מדף אלון, פינת משחק |
| אותיות (1) | tracehe | נייר קרם אפוי על שולחן עץ, הנחיה שדוהה לפי 4 הדרגות |

**סה"כ ארט חדש:** ~870KB. כל משחק עם `scripts/gen-<game>-art.mjs` ו-`LICENSES.md` (CC0 מקורי).

### אימות שעשיתי בעצמי (לא הסתמכתי על דיווחי הסוכנים)
- tsc + build נקיים אחרי כל מיזוג.
- **צילמתי והסתכלתי על כל 13 המשחקים** ב-360×780.
- **אפס רגרסיה בנגישות:** משחקים עם חפיפת מגע נשארו 7 (בסיס 9), גלישה אופקית 0, ומספר הכפתורים שעומדים ברף **עלה** 305→306.
- ⚠️ האחוז ירד 35%→31% **רק כי המכנה גדל** — 13 המשחקים החדשים הוסיפו ~102 כפתורים. שום מטרה קיימת לא הוקטנה.

### באג אמיתי שנמצא ואומת
`@napi-rs/canvas` — `toBuffer('image/jpeg', q)` דורש **מספר שלם 0-100**, לא שבר.
אימתתי בעצמי: עם `0.95` חום `rgb(139,107,69)` חוזר כ-`rgb(141,84,39)`; עם `95` מדויק.
**`scripts/gen-drawnum-art.mjs` עדיין משתמש ב-`0.95`.** הארט שלו כבר פורסם והגרעין הכבד מסווה את זה — לא נגעתי. **מועמד לתיקון עתידי.**

### חוב שנוצר בדרך (לידיעה)
ארבעת משחקי "גדלים" נבנו ע"י סוכנים מקבילים ש**חלקו worktree אחד ודרסו זה את זה**. התוצאה: ה-CSS של `schedule` נחת בתוך הקומיט של `feelings` (`cea1266`) במקום בשלו (`6c2f6fd`). **התוכן נכון ואומת על המסך** — רק חלוקת הקומיטים לא מסודרת. **לקח: worktree נפרד לכל סוכן, תמיד.**

---

## 2026-08-02 — גל נגישות מגע (בראנץ׳ `a11y-touch-targets`)

### מצב נוכחי
- בראנץ׳ `a11y-touch-targets` מסועף מ-`quality-wave` (= `main` = `origin/main` = `e8750c6`, תג v38).
- **ארבעה קומיטים**, tsc + build עוברים, **לא פורסם** (הדחיפה ידנית ע"י גיתית):
  - `2033365` שכבה א׳ — הקצה המשותף
  - `1e4fa77` תיעוד
  - `f06ed71` שכבה ב׳ — רצפות clamp, המחשבון, הרחבות מגע
  - `2867067` שכבה ג׳ — פסנתר וגירי הציור
- שרת תצוגה מקדימה לבדיקה: `npx vite preview --port 6321`.

### התוצאה במספרים (נמדד ב-Chromium, 360px, לא חושב מה-CSS)
מדד: אחוז הכפתורים האמיתיים (`<button>`/`role=button`) שעומדים ב-72px בשני הצירים,
על פני כל 63 המשחקים.

| | לפני | אחרי |
|---|---|---|
| כפתורים ברף | **86 (9%)** | **305 (35%)** |
| משחקים עם חפיפת מגע | 9 | **7** |
| גלישה אופקית | 0 | 0 |
| משחקים שאיבדו שטח משחק | — | 4 (‎-8% עד ‎-21%) |

**הערה חשובה למדידה:** המונה סופר גם `<button>` דקורטיביים מקוננים (איברי הדמויות —
`bump`, `earring`, `pigtail`), ולכן האחוז המוחלט נמוך מהאמת. **ההשוואה בין לפני
לאחרי היא המספר בעל המשמעות** — אותה מתודה בדיוק בשני הצדדים.

### מה הביא לגל הזה
גיתית זיהתה שהמשחקים עוצבו יפה אבל **פספסו נגישות לילד** — לחצני המחשבון הוקטנו לו.

**הרף שנקבע (החלטת בעלים, 2026-08-02): 72×72px** במסך 360px, לפי מחקר מגע לגיל 4-6.
תקני מבוגרים (WCAG 44px, Material 48px) **אינם מספיקים בריפו הזה**.

### ממצאי הביקורת — 68 כשלים מתוך 100 מטרות
ארבעה סוכנים read-only סרקו את כל המשחקים; המספרים הקריטיים אומתו ידנית.

**תיקון להנחה המקורית:** הארט האפוי כמעט ולא כיווץ מטרות. **רק 4 רגרסיות אמיתיות** מהגל:
| משחק | לפני → אחרי |
|---|---|
| `calc` `.calc-key` | 72.5 → 54.4 |
| `pattern` `.pat-choice` | 61.2 → 43.2 |
| `missing` `.miss-tile` | ~90 → 58 |
| `drawboard` `.crayon-swatch` | 40 → 34 |

**64 הכשלים הנותרים קדמו לגל** — הגל אפילו הגדיל חלק (letterhunt 48→56, nametower 58→66, פסנתר 66→70).

**שתי סיבות שורש:**
1. לא היה טוקן מגע ב-`:root` — שום מעקה.
2. רצפות `clamp(56px, 15.5vw, 82px)` נקבעו לפי תקן מבוגרים. **ב-360px רכיב ה-vw תמיד מפסיד, כך שהרצפה היא הגודל האמיתי.**

### מה נסגר בשכבה א׳ (קומיט `2033365`)
ב-`src/app.css`:
- טוקנים חדשים `--tap-min: 72px` / `--tap-adult: 48px` + מחלקת עזר `.tap-safe`.
- `.pill` / `.pill-small` → `min-height: 72` (היו ~35-41px). גדלים **ויזואלית**, כי הם נראו קטן מדי.
- `.big-button` → 60 → 72.
- `.stepper-arrow` → דיסקה 46 → **56px**.
- `.control-btn` / `.btn-utility` / `.icon-button` → הדיסקה נשארת בגודלה, **אזור המגע** מורחב ב-`::after`.

### מה נמדד בפועל (Chromium, 360px — לא רק חישוב)
- **ציר הגובה: 72px מלאים בכל הפקדים המשותפים.** ✅
- **ציר הרוחב לא נסגר ולא ניתן לסגירה בלייאאוט הנוכחי:**
  `.control-btn` 64-66px · `.btn-utility` 56px.
  **הסיבה:** 5 פקדים חולקים שורה של 328px. 72px אמיתי לכל אחד דורש 400px+.
- ⚠️ **טעות שנתפסה בבדיקה ותוקנה:** ניסיון ראשון נתן מלבני מגע ברוחב 72px מלא → **חפיפה של 6-14px** בין כפתורים שכנים, כלומר כפתור גונב הקלקות משכנו — גרוע ממטרה קטנה. המלבנים נחתכים כעת לדיסקה+רווח. **אומת: 0 חפיפות ב-12 משחקים.**

### מה נסגר בשכבות ב׳+ג׳
- **המחשבון (מה שגיתית דיווחה):** מקש 54 → **62px** צבוע + הילה 6px. `.calc-device` שוחרר
  מ-300px ל-344px והריפוד/רווח קוצצו. הגוף האפוי הוא רקע נמתח, אז ההגדלה מסקיילת את הארט.
  **נבדק בצילום** — המקשים יושבים בתוך מסילות הגוף.
- **16 רצפות clamp הועלו ל-72px** (הן היו הגודל האמיתי במסך 360px).
- `.btn-primary` 52 → 72 · `.btn-secondary` 44 → 72.
- **`letterhunt`:** 8 נקודות הפיזור פוזרו מחדש (3-2-3). ההערה בקוד טענה שהבלוקים
  "לעולם לא חופפים" — **המדידה הראתה שהם חפפו כבר ב-56px.**
- **פסנתר:** 54×70 → **58×88**. 6 נתיבים ב-328px לא יכולים להיות 72 ברוחב, ו**המנגינות
  משתמשות בכל 6 הדרגות** — אז הורדת נתיב הייתה שוברת שירים. הגובה הוא הציר החופשי.
- **גירי ציור:** 34×78 → **36×84** (היו המטרה הצרה באפליקציה).

### שכבה ד׳ — סחיפה על כל שאר המשחקים (קומיט `c621cab`)
הוגדלו לרף: `qty-replay`, `dance-swap`, `syl-hear`, `tr-num`, `ice-tub`, `cake-tub`,
`ice-top-btn`, `cake-decor-btn`, `die-type-chip`, `build-op-btn`, `rb-block-btn`,
`memory-card`, `color-swatch`, `ss-chip`, `dance-ctrl` + עוד ~30 דרך `min-height`.

#### ⚠️ הלקח החשוב ביותר של הגל: שומר שטח המשחק
הגדלת פקדים **אינה חינם**. בחלק מהמשחקים לוח הבקרה יושב **מעל** הלוח, ולכן כל
פיקסל שהפקדים מרוויחים נגזל מהמשחק עצמו. מדידת שטח המשחק לפני/אחרי תפסה
**חמש רגרסיות שאני עצמי יצרתי**:
- `drawboard` — **הקנבס קרס ל-0px** (‎`flex-wrap` דחף את `.color-actions` לשתי שורות)
- `rhythmblocks` ‎-37% · `colorbynumber` ‎-36% · `band` ‎-20% · `sort` ‎-15%

**התיקון:** pill מוקטן ל-56px בלוחות בקרה צפופים (`.rb-levels`, `.band-levels`,
`.cbn-filter`, `.sort-modes`), stepper/legend קומפקטיים ב-`.color-screen`, ומגש
הציור הוחזר לגובה המקורי. **פקד קטן יותר שהילד עדיין פוגע בו עדיף על פקד גדול
במסך שבו המשחק כבר לא נכנס.**

**נשאר:** `sort` ‎-8% · `band` ‎-10% · `cbn` ‎-12% · `rhythm` ‎-21% — **כל הארבעה
נבדקו בצילום ומשחקים תקין.**

**כלל עבודה להמשך:** אחרי כל שינוי גודל פקד — להריץ `playarea.mjs` ולהשוות מול
`quality-wave`. בילד ירוק לא מעיד על כלום.

### מה נשאר — לפי סדר
0. **העברת לוחות הבקרה הצפופים לשׁיט/מגירה** — זה מה שיסגור סופית גם את הרף
   וגם את שטח המשחק ב-4 המשחקים למעלה. שינוי עיצובי, דורש אישור.
1. **החלטה עיצובית פתוחה — שורת הכותרת.** סגירת ציר הרוחב מחייבת **פחות כפתורים בשורה**
   (כיום 5: חזרה/בית/קול/רגוע/דרגה). `.control-btn` תקוע ב-64-66px רוחב. לא לבצע בלי אישור גיתית.
2. **מקרים מבניים שנשארו פתוחים:**
   - `colorbynumber` — תא 27px (16 עמודות). חייב זום.
   - `catch` — `transform: scale()` מכווץ את אזור הלחיצה (32px, **מטרה נעה**).
   - `coinsort` `.cs-tube` — 62px רוחב, נעול לארט של המגש.
   - `soundwheel` `.sw-seg` — פלחי גלגל שחופפים מטבעם.
   - `dice` — 80px בקובייה אחת, יורד ל-40px ככל שמוסיפים.
   - `garden` (48px רוחב) / `letterdrawer` — כלואים במסגרת הארט האפוי.
3. **QA של גיתית** על 49 המשחקים ששודרגו — [טרקר](https://claude.ai/code/artifact/5ba64cfd-0b7d-411c-bf0b-891177753b0e).
4. **13 משחקים שלא שודרגו:** `tracehe` · אנגלית (6) · גדלים (4) · מוזיקה (2).
5. **ניקיון:** ~89 worktrees ישנים של סוכנים מהגל הלילי.

### קבצים ופקודות
- כל התיקונים: `src/app.css` (הטוקנים ב-`:root`, ~שורה 45).
- קצה משותף: `src/components/GameShell.tsx`, `LevelButton.tsx`.
- בדיקה: `node node_modules/typescript/lib/tsc.js --noEmit` (שים ה-`.bin/tsc` לפעמים תקול) ואז `npx vite build`.
- סקריפטי המדידה בדפדפן (scratchpad): `measure-taps.mjs`, `check-overlap.mjs` — דורשים `npm install playwright` מקומי ושרת על 6321.

### מוקשים
- **אל תסמכי על "הבילד ירוק" כהוכחה לנגישות.** הבאג של החפיפה עבר tsc+build בהצלחה מלאה והתגלה רק במדידה בדפדפן.
- **הרחבת אזור מגע חייבת בדיקת חפיפה.** כל הרחבה מעבר לרווח בין הפקדים = גניבת הקלקות.
- פורטים אסורים: 5173, 5174, 5199, 5202, 6199.
- הדחיפות שלי חסומות למסווג — הפרסום ידני ע"י גיתית: `git push origin main --follow-tags`.
